{% extends 'base.html.twig' %}

{% block body %}
    <h1>Stundeneintrag creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('stundeneintrag_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}

{% extends 'base.html.twig' %}

{% block body %}
    <h1>Materialeintrag erstellen</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class ="btn btn-success" value="Neuer Eintrag" />
    {{ form_end(form) }}

    <li class="list-inline">
            <a class="btn btn-info" href="{{ path('arbeitsrapport_show', { 'id': id }) }}">Zurück</a>
 	 </li>
{% endblock %}

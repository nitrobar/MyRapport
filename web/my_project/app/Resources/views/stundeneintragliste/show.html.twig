{% extends 'base.html.twig' %}

{% block body %}
    <h1>Stundeneintragliste</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stundeneintragliste.id }}</td>
            </tr>
            <tr>
                <th>Datum</th>
                <td>{% if stundeneintragliste.datum %}{{ stundeneintragliste.datum|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Leistung</th>
                <td>{{ stundeneintragliste.leistung }}</td>
            </tr>
            <tr>
                <th>Std</th>
                <td>{{ stundeneintragliste.std }}</td>
            </tr>
            <tr>
                <th>Betragprostd</th>
                <td>{{ stundeneintragliste.betragProStd }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ stundeneintragliste.total }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('stundeneintragliste_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('stundeneintragliste_edit', { 'id': stundeneintragliste.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
